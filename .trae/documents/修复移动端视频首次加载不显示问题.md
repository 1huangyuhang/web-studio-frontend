1. **问题分析**：视频事件监听器添加顺序错误，导致初始加载事件被错过，视频保持不可见
2. **修复方案**：在`/Users/huangyuhang/Downloads/Test/前端/src/animations/components/ScrollyVideo.tsx`文件中，重新排序视频事件处理useEffect中的代码
3. **关键修改**：将事件监听器的添加操作移到调用video.load()和video.play()之前，确保所有事件都能被正确捕获
4. **验证方法**：在移动端直接访问首页，检查视频是否能正常显示，无需通过菜单栏切换页面
5. **注意事项**：仅修改移动端相关代码，保持桌面端功能不变

修改前顺序：

1. 定义事件处理函数
2. 调用video.load()和video.play()
3. 添加事件监听器

修改后顺序：

1. 定义事件处理函数
2. 添加事件监听器
3. 调用video.load()和video.play()

