# 本地开发端口（修改此处后，官网 / 管理端 / API 代理会一并生效）
# 可被 scripts/*.sh source，也可在启动前 export 覆盖
#
# REDWOOD_PORT_API：若启动前未 export 该变量，会先读 backend/.env 的 PORT，再读本文件。
# ECONNREFUSED 127.0.0.1:xxxx 表示该端口上没有后端进程，请先在本仓库运行后端：npm run dev:backend
REDWOOD_PORT_SITE=5173
REDWOOD_PORT_MANAGEMENT=3001
REDWOOD_PORT_API=3000
